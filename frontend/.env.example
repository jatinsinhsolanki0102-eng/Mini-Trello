# Copy to ".env" and adjust if the backend is not running on the default URL.
# By default the frontend talks to "/api" through the Vite dev proxy,
# so an explicit value is only needed when serving the built app separately.
VITE_API_URL=http://localhost:5000/api